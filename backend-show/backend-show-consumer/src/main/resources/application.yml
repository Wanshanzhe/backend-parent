server:
  port: 7201

spring:
  application:
    name: hello-service-consumer

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

logging:
  config: classpath:logback.xml
